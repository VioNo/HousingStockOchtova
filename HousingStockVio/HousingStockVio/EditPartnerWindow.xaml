<Window x:Class="HousingStockVio.EditPartnerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Партнер" 
        Height="500" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="Resources/icon.ico">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" 
                Background="{StaticResource AccentBrush}" 
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,15">
            <TextBlock x:Name="HeaderText" 
                      Text="Форма партнера" 
                      FontSize="18" 
                      FontWeight="Bold"
                      Foreground="White"
                      HorizontalAlignment="Center"/>
        </Border>

        <!-- Поля формы -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto"
                     Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Название партнера -->
                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <Label Content="Название партнера *" 
                          FontWeight="Bold"
                          Foreground="#333333"/>
                    <TextBox x:Name="NameBox" 
                            Height="35"
                            Margin="0,5,0,0"
                            FontSize="14"
                            Padding="5"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderBrush}"
                            ToolTip="Введите полное наименование партнера"/>
                </StackPanel>

                <!-- Контактное лицо -->
                <StackPanel Grid.Row="1" Margin="0,0,0,15">
                    <Label Content="Контактное лицо *" 
                          FontWeight="Bold"
                          Foreground="#333333"/>
                    <TextBox x:Name="ContactBox" 
                            Height="35"
                            Margin="0,5,0,0"
                            FontSize="14"
                            Padding="5"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderBrush}"
                            ToolTip="Введите ФИО контактного лица"/>
                </StackPanel>

                <!-- Телефон -->
                <StackPanel Grid.Row="2" Margin="0,0,0,15">
                    <Label Content="Телефон" 
                          FontWeight="Bold"
                          Foreground="#333333"/>
                    <TextBox x:Name="PhoneBox" 
                            Height="35"
                            Margin="0,5,0,0"
                            FontSize="14"
                            Padding="5"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderBrush}"
                            ToolTip="Введите контактный телефон"/>
                </StackPanel>

                <!-- Email -->
                <StackPanel Grid.Row="3" Margin="0,0,0,15">
                    <Label Content="Email" 
                          FontWeight="Bold"
                          Foreground="#333333"/>
                    <TextBox x:Name="EmailBox" 
                            Height="35"
                            Margin="0,5,0,0"
                            FontSize="14"
                            Padding="5"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderBrush}"
                            ToolTip="Введите email адрес"/>
                </StackPanel>

                <!-- Услуги -->
                <StackPanel Grid.Row="4" Margin="0,0,0,15">
                    <Label Content="Услуги" 
                          FontWeight="Bold"
                          Foreground="#333333"/>
                    <TextBox x:Name="ServicesBox" 
                            Height="60"
                            Margin="0,5,0,0"
                            FontSize="14"
                            Padding="5"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderBrush}"
                            TextWrapping="Wrap"
                            AcceptsReturn="True"
                            VerticalScrollBarVisibility="Auto"
                            ToolTip="Опишите предоставляемые услуги"/>
                </StackPanel>

                <!-- Номер договора -->
                <StackPanel Grid.Row="5" Margin="0,0,0,15">
                    <Label Content="Номер договора" 
                          FontWeight="Bold"
                          Foreground="#333333"/>
                    <TextBox x:Name="ContractNumberBox" 
                            Height="35"
                            Margin="0,5,0,0"
                            FontSize="14"
                            Padding="5"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderBrush}"
                            ToolTip="Введите номер договора"/>
                </StackPanel>

                <!-- Дата договора -->
                <StackPanel Grid.Row="6" Margin="0,0,0,15">
                    <Label Content="Дата договора" 
                          FontWeight="Bold"
                          Foreground="#333333"/>
                    <DatePicker x:Name="ContractDateBox" 
                               Height="35"
                               Margin="0,5,0,0"
                               FontSize="14"
                               Padding="5"
                               BorderThickness="1"
                               BorderBrush="{StaticResource BorderBrush}"
                               SelectedDateFormat="Short"/>
                </StackPanel>

                <!-- Статус -->
                <StackPanel Grid.Row="7">
                    <Label Content="Статус *" 
                          FontWeight="Bold"
                          Foreground="#333333"/>
                    <ComboBox x:Name="StatusBox" 
                             Height="35"
                             Margin="0,5,0,0"
                             FontSize="14"
                             Padding="5"
                            BorderThickness="1"
                            BorderBrush="{StaticResource BorderBrush}"
                            ToolTip="Выберите статус партнера">
                        <ComboBoxItem Content="Активен"/>
                        <ComboBoxItem Content="Неактивен"/>
                        <ComboBoxItem Content="Приостановлен"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">

            <Button x:Name="SaveButton" 
                    Content="Сохранить" 
                    Style="{StaticResource PrimaryButton}"
                    Width="120"
                    Height="40"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Click="SaveButton_Click"/>

            <Button x:Name="CancelButton" 
                    Content="Отмена" 
                    Style="{StaticResource SecondaryButton}"
                    Width="120"
                    Height="40"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Margin="10,0,0,0"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>