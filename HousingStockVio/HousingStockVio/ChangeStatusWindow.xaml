<Window x:Class="HousingStockVio.ChangeStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Изменение статуса заявки" 
        Height="300" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Icon="Resources/icon.ico">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <Border Grid.Row="0" 
                Background="{StaticResource AccentBrush}" 
                CornerRadius="5"
                Padding="10"
                Margin="0,0,0,15">
            <TextBlock Text="Изменение статуса заявки" 
                      FontSize="18" 
                      FontWeight="Bold"
                      Foreground="White"
                      HorizontalAlignment="Center"/>
        </Border>

        <!-- Информация о заявке -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock x:Name="ApplicationInfoText"
                      TextWrapping="Wrap"
                      FontSize="14"/>
        </StackPanel>

        <!-- Новый статус -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <Label Content="Новый статус *" 
                  FontWeight="Bold"
                  Foreground="#333333"/>
            <ComboBox x:Name="StatusComboBox" 
                     Height="35"
                     Margin="0,5,0,0"
                     FontSize="14"
                     Padding="5"
                     BorderThickness="1"
                     BorderBrush="{StaticResource BorderBrush}">
                <ComboBoxItem Content="В работе"/>
                <ComboBoxItem Content="Завершена"/>
                <ComboBoxItem Content="Отложена"/>
            </ComboBox>
        </StackPanel>

        <!-- Комментарий -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <Label Content="Комментарий (необязательно)" 
                  FontWeight="Bold"
                  Foreground="#333333"/>
            <TextBox x:Name="CommentBox" 
                    Height="60"
                    Margin="0,5,0,0"
                    FontSize="14"
                    Padding="5"
                    BorderThickness="1"
                    BorderBrush="{StaticResource BorderBrush}"
                    TextWrapping="Wrap"
                    AcceptsReturn="True"
                    VerticalScrollBarVisibility="Auto"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">

            <Button x:Name="SaveButton" 
                    Content="Сохранить" 
                    Style="{StaticResource PrimaryButton}"
                    Width="120"
                    Height="40"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Click="SaveButton_Click"/>

            <Button x:Name="CancelButton" 
                    Content="Отмена" 
                    Style="{StaticResource SecondaryButton}"
                    Width="120"
                    Height="40"
                    FontSize="14"
                    FontWeight="SemiBold"
                    Margin="10,0,0,0"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>